apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "db-dev-pvc-0"
  namespace: "application-dev"
spec:
  accessModes:
    - "ReadWriteMany"
  storageClassName: ""
  volumeName: "db-dev-pv-0"
  resources:
    storage: "10Gi"
