apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: "qr-frontend-admin-dev"
  namespace: "application-dev"
  labels:
    application: "frontend"
    stage: "dev"
spec:
  replicas: 1
  strategy:
    blueGreen:
      autoPromotionEnabled: true
      autoPromotionSeconds: 30
      maxUnavailable: 1
      activeService: "frontend-admin-dev-svc"
      scaleDownDelaySeconds: 10
  selector:
    matchLabels:
      application: "frontend-admin"
      stage: "dev"
  template:
    metadata:
      labels:
        application: "frontend-admin"
        stage: "dev"
    spec:
      restartPolicy: Always
      containers:
        - name: frontend-dev
          image: qrsmartmenu/frontend-admin:0.1.0-rc3
          imagePullPolicy: IfNotPresent
          ports:
            - name: front-adm-port
              containerPort: 3000
              protocol: "TCP"
          env:
            - name: BASE_URL
              valueFrom:
                configMapKeyRef:
                  key: "API_URL"
                  name: "backend-app-dev-cm"
