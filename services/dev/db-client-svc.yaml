apiVersion: v1
kind: Service
metadata:
  name: db-client-svc
  namespace: application-dev
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: db-client-port
      protocol: "TCP"
  selector:
    app: adminer